import 'package:flutter/material.dart';

/// App languages supported via easy_localization.
const supportedAppLocales = <Locale>[
  Locale('en'),
  Locale('bn'),
  Locale('hi'),
];

const translationsPath = 'assets/translations';
